from setuptools import setup, find_packages

setup(
    name='doc_ingestion',
    version='0.1',
    description='A package for ingesting and processing documents',
    author='Giacomo Signorile',
    packages=find_packages(),
    python_requires='>=3.9',
)
